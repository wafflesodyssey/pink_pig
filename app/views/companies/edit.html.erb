<h1>Edit a company</h1>
<%= form_for @company, url: company_path(id: @company.id), method: :patch do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label "Distributor?" %>
  <%= f.select :distributor, ["true", "false"] %>
  <%= f.submit "Save" %>

<% end %>
